<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Catálogo de Productos</title>
</head>
<body>

    <h1>Catálogo de Productos</h1>
    <hr>
    
    <div>
        <span>Página {{currentPage}} de {{totalPages}}</span>
    </div>

    <div>
        {{#each products}}
        <div>
            <h2>{{title}}</h2>
            <p>{{description}}</p>
            <p>${{price}}</p>
            <p>Stock: {{stock}} | Categoría: {{category}}</p>
            
            <div>
                <a href="/products/{{_id}}">
                    Ver Detalle
                </a>
                <button 
                    onclick="alert('Funcionalidad de agregar al carrito (ID: {{_id}}) aún no implementada en el cliente.')">
                    Añadir al Carrito
                </button>
            </div>
        </div>
        {{else}}
        <p>No se encontraron productos en el catálogo.</p>
        {{/each}}
    </div>

    <div>
        
        {{#if hasPrevPage}}
            <a href="{{prevLink}}">
                Página Anterior
            </a>
        {{else}}
            <span>Página Anterior</span>
        {{/if}}

        <span>{{currentPage}}</span>

        {{#if hasNextPage}}
            <a href="{{nextLink}}">
                Página Siguiente
            </a>
        {{else}}
            <span>Página Siguiente</span>
        {{/if}}
    </div>

</body>
</html>