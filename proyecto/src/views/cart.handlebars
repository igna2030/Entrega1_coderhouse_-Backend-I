<!DOCTYPE html>
<html>
<head>
    <title>Carrito de Compras</title>
</head>
<body>

    <h1 style="text-align: center; margin-bottom: 20px;">Contenido del Carrito</h1>
    <hr>
    
    <p style="text-align: center; color: #666; margin-bottom: 30px;">
        ID del Carrito: <span style="font-family: monospace; background-color: #eee; padding: 2px 5px; border: 1px solid #ccc;">{{cart._id}}</span>
    </p>

    {{#if products}}
    <div style="display: flex; flex-direction: column; gap: 15px; max-width: 600px; margin: 0 auto;">
        {{#each products}}
        <div style="background-color: white; padding: 15px; border: 1px solid #ddd; box-shadow: 2px 2px 5px rgba(0,0,0,0.1); display: flex; align-items: center; justify-content: space-between;">
            
            <div style="flex-grow: 1; min-width: 0;">
                <h3 style="margin: 0; font-size: 1.1em; color: #333; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">
                    {{productId.title}}
                </h3>
                <p style="margin: 5px 0 0 0; font-size: 0.8em; color: #888;">ID: {{productId._id}}</p>
                <p style="margin: 5px 0 0 0; font-size: 1.2em; font-weight: bold; color: green;">${{productId.price}}</p>
            </div>
            
            <div style="display: flex; align-items: center; gap: 20px; margin-left: 20px;">
                <div style="text-align: right;">
                    <span style="font-weight: bold; color: #333;">Cantidad:</span>
                    <span style="font-size: 1.5em; font-weight: bold; color: indigo;">{{quantity}}</span>
                </div>

                <button 
                    style="background-color: red; color: white; padding: 8px 12px; border: none; cursor: pointer;"
                    onclick="alert('Funcionalidad para eliminar producto {{productId.title}} (DELETE) aún no implementada en el cliente.')">
                    Eliminar
                </button>
            </div>
        </div>
        {{/each}}
    </div>
    
    <div style="margin-top: 30px; text-align: center;">
        <button 
            style="background-color: indigo; color: white; font-size: 1.1em; font-weight: bold; padding: 12px 25px; border: none; cursor: pointer; box-shadow: 0 4px 6px rgba(0,0,0,0.1);"
            onclick="alert('Funcionalidad para finalizar compra aún no implementada.')">
            Finalizar Compra
        </button>
    </div>

    {{else}}
    <div style="max-width: 400px; margin: 0 auto; padding: 15px; border: 1px solid orange; background-color: #fff8e1; color: #664400;">
        <p style="font-weight: bold; margin: 0;">Carrito Vacío</p>
        <p style="margin-top: 5px;">Aún no hay productos en este carrito.</p>
    </div>
    {{/if}}

</body>
</html>